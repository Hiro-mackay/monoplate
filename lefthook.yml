#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-push:
  commands:
    build:
      run: pnpm build

pre-commit:
  commands:
    typecheck:
      run: pnpm typecheck
    format:
      run: pnpm format
